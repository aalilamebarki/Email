# المراجعة الشاملة والدليل المعماري الفني لمنصة FreeTemp.email
> **وثيقة تدقيق هندسي ومراجعة معمارية شاملة لجميع ملفات ومكونات المشروع**  
> **التاريخ:** سبتمبر 2026 | **النطاق المعتمد:** `freetemp.email` | **الإصدار:** v2.4.0 Production Ready

---

## 1. الملخص التنفيذي والرؤية الهندسية (Executive Summary & Mission)

منصة **FreeTemp.email** هي نظام متكامل وعالي الأداء لتقديم خدمة **البريد المؤقت (Disposable Temporary Email)** الفوري بدون تسجيل أو جمع أي بيانات شخصية، مبني على أحدث معايير الويب الموزع (Edge Computing) والواجهات فائقة السرعة المعتمدة على بنية **Static JAMstack + Edge Worker + Durable Objects**.

### الأهداف المحققة في المشروع:
1. **الخصوصية المطلقة وانعدام التتبع (Zero-Knowledge & Privacy-First):** لا توجد قواعد بيانات مركزية دائمة تحتفظ بهويات المستخدمين، ويتم تشفير وفصل كل صندوق بريد بصورة تامة.
2. **الاستجابة الحية الفورية (Real-time Latency):** استقبال البريد وبثه إلى متصفح المستخدم في أقل من **500 ميلي ثانية** عبر تقنية **WebSocket Hibernation API**.
3. **محرك الاستخراج الذكي المزدوج (Universal Verification Coverage Engine):** استخراج تلقائي ودقيق لرموز التحقق (OTP) بكافة أشكالها (4، 6، 8 أرقام، رموز Google G-Tokens، والرموز الأبجدية الرقمية) وروابط التفعيل الآمنة مع حجب روابط إلغاء الاشتراك أو الحذف التلقائي.
4. **جدار الحماية من بكسلات التجسس (Spy Pixel Shield):** حجب تحميل الصور والعناصر الخارجية تلقائياً لمنع مرسلي البريد من تتبع عنوان IP الجغرافي للمستخدم أو معرفة توقيت فتح الرسالة، مع تمكين زر اختياري لعرض الصور بأمان.
5. **إمبراطورية سيو تقنية عالمية (Global SEO Architecture):** دعم كامل لـ **22 لغة عالمية** في واجهة المستخدم، وتوليد مستقل لـ **29 مسار توجيه محلي**، بالإضافة إلى **92 مقالاً هندسياً معمقاً** تغطي كافة أبعاد أمن البريد الرقمي، مع ربط دقيق بخرائط الموقع `sitemap.xml` وبيانات Schema.org المنظمة.

---

## 2. الهندسة المعمارية وتدفق البيانات (System Architecture & Data Flow)

```
[مرسل البريد الخارجي / SMTP Sender]
                 │
                 ▼
[Cloudflare MX Records & Email Routing: mx.freetemp.email]
                 │
                 ▼
[Edge Worker: worker/src/index.ts (email handler)]
                 │
                 ▼ (PostalMime Parser: OOM Protection & MIME Extraction)
                 │
                 ▼ (Deterministic O(1) Routing: env.MAILBOX.idFromName(address))
[Durable Object: worker/src/mailbox-do.ts (Per-Mailbox Isolated Instance)]
                 │
                 ├──► [In-Memory SQLite Storage (Max 50 Emails, Encrypted)]
                 ├──► [Scheduled Alarm: Expiry Timer (20 Mins Auto-Destroy)]
                 └──► [WebSocket Broadcast via Hibernation API]
                                 │
                                 ▼ (WSS Stream)
           [متصفح المستخدم / Client Browser: shared/app.js]
                 │
                 ├──► [DOMPurify Sanitization & Sandbox]
                 ├──► [Web Audio API Sound Alert & Haptic Feedback]
                 └──► [One-Click OTP & Direct Activation Link Extraction]
```

### المبادئ المعمارية الجوهرية:
- **العزل الحتمي $O(1)$:** يتم توليد معرّف Durable Object بناءً على عنوان البريد مباشرة عبر `env.MAILBOX.idFromName(email)`. هذا يضمن وصول الرسالة فوراً إلى نفس كائن الذاكرة المستقل الخاص بالمستخدم دون الحاجة لاستعلام قواعد بيانات أو جداول بحث.
- **التخزين المؤقت والجدولة الدقيقة (Stateful Alarms):** بمجرد إنشاء الصندوق أو تلقي أول رسالة، يتم تفعيل جدولة زمنية `state.storage.setAlarm()` لإتلاف الصندوق وحذف كافة محتوياته نهائياً من الذاكرة بمجرد انتهاء الـ 20 دقيقة، ما لم يقم المستخدم بالضغط على زر التمديد (+10 دقائق).
- **منع التجسس والاختراق عبر التوكن (Token Guard):** عند توليد العنوان لأول مرة، يُنتج النظام مفتاح وصول سري عشوائي مشفر `secretToken`. لا يستطيع أي طرف ثالث استعراض الرسائل أو فتح اتصال WebSocket إلا إذا كان يحمل هذا التوكن الموثق.

---

## 3. الدراسة التفصيلية لملفات المشروع (Exhaustive File-by-File Audit)

### أولاً: محرك الحوسبة السحابية (Cloudflare Workers & Backend)

#### 1. `worker/wrangler.toml`
- **الدور:** ملف إعداد ونشر الخادم السحابي عبر منصة Cloudflare Workers.
- **المعلمات الأساسية:**
  - `name = "temp-mail"`: اسم الخدمة السحابية.
  - `main = "src/index.ts"`: ملف المدخل الرئيسي للتطبيق السحابي.
  - `compatibility_date = "2024-09-23"` و `compatibility_flags = ["nodejs_compat"]`: تفعيل توافقية دوال Node.js لمعالجة تدفقات Buffer و Streams.
  - `[vars] DOMAIN = "freetemp.email"`: تحديد النطاق الرئيسي للمنصة.
  - `EXPIRY_MINUTES = "20"`: مدة صلاحية صندوق البريد الافتراضية.
  - `[[durable_objects.bindings]] name = "MAILBOX", class_name = "MailboxDO"`: ربط كلاس الكائنات الموزعة ذات الحالة المستمرة.
  - `[[migrations]] tag = "v1", new_sqlite_classes = ["MailboxDO"]`: استخدام محرك SQLite فائق السرعة والمدمج داخل Durable Objects.

#### 2. `worker/src/index.ts`
- **الدور:** بوابة الدخول المركزية (Central Gateway) للطلبات السحابية بشقيها: بروتوكول HTTP/REST/WebSocket، وبروتوكول استقبال رسائل البريد الإلكتروني (Cloudflare Email Routing).
- **الوظائف والمكونات البرمجية:**
  - `getCorsHeaders(request, env)`: دالة تحصين الـ CORS التي تسمح بالطلبات الواردة من `freetemp.email` وبيئات التطوير مع ضبط الترويسات الأمنية الصارمة (`nosniff`, `DENY`, `strict-origin-when-cross-origin`, و CSP صارمة).
  - `checkRateLimit(ip)`: محدد استهلاك ومعدل طلبات في الذاكرة (10 طلبات إنشاء/تحديث لكل 10 دقائق لكل IP) لمنع استنزاف الموارد وهجمات DoS.
  - `email(message, env, ctx)`: معالج البريد الوارد المباشر من خوادم البريد العالمية:
    - فحص حجم الرسالة ورفض الحزم التي تتجاوز الحد الأقصى لحماية الذاكرة من الإفراط (OOM Prevention).
    - استخدام مكتبة `postal-mime` لتحويل نص البريد الخام (MIME) إلى كائن منظم يحتوي على المرسل، المستقبل، الموضوع، النص الصريح، وكود الـ HTML.
    - استدعاء `extractOtp()` و `extractLinks()` لاستخراج كود التحقق والروابط المباشرة في لحظة الاستلام وقبل إرسالها إلى التخزين.
    - تمرير الحزمة عبر طلب داخلي آمن `/internal/email` إلى Durable Object الخاص بالصندوق.
  - `fetch(request, env, ctx)`: معالج واجهة برمجة التطبيقات (API) واتصالات الويب سوكت:
    - `/api/new`: توليد بريد إلكتروني جديد فوراً مع توكن أمان فريد وتحديد تاريخ الانتهاء.
    - `/api/emails`: جلب قائمة الرسائل مع التحقق الصارم من التوكن (`Authorization: Bearer`).
    - `/api/ws`: ترقية الاتصال (Protocol Upgrade 101) إلى اتصال WebSocket حي ومباشر عبر تقنية Hibernation API.
    - `/api/extend`: تمديد صلاحية الصندوق 10 دقائق إضافية.
    - `/api/burn`: الحرق الفوري الشامل لكافة الرسائل وإغلاق الصندوق نهائياً.

#### 3. `worker/src/mailbox-do.ts`
- **الدور:** الكلاس البرمجي لكائن الحالة الموزع `MailboxDO` (Durable Object Instance).
- **الوظائف البرمجية الدقيقة:**
  - `constructor(state, env)`: تهيئة الكائن وربط التخزين ومؤقت الصلاحية.
  - `alarm()`: المعالج الزمني الذي تستدعيه سحابة Cloudflare تلقائياً عند انتهاء وقت الصندوق لتنفيذ `state.storage.deleteAll()` وتدمير كافة البيانات نهائياً.
  - `webSocketMessage(ws, message)`: معالج أحداث الويب سوكت للرد على رسائل الفحص (Ping/Pong) واستقبال أوامر التمديد أو الحرق المباشرة من المتصفح.
  - `webSocketClose(ws)`: تنظيف وإدارة الاتصالات المغلقة.
  - بث الرسائل الجديدة لجميع الجلسات المفتوحة للمستخدم المتصل بنفس الصندوق فور وصولها عبر `ws.send()`.

#### 4. `worker/src/parser.ts`
- **الدور:** محرك فك وتفكيك محتوى الرسائل واستخراج رموز التحقق وروابط الأمان.
- **المنطق التقني (Heuristics & Regex):**
  - **استخراج الـ OTP:**
    - فحص رموز Google المعتمدة مثل `G-123456`.
    - فحص الرموز المقسمة بالأشكال `123-456` أو `123 456`.
    - فحص الرموز الرقمية المكونة من 4 إلى 8 خانات المتزامنة مع كلمات مفتاحية (code, verification, otp, token, pin, تأكيد, رمز, كود).
    - فحص الرموز الأبجدية الرقمية لحسابات Steam أو Discord.
  - **استخراج الروابط:**
    - استخراج روابط التفعيل الصريحة (`verify`, `confirm`, `activate`, `validate`).
    - استبعاد وفلترة روابط إلغاء الاشتراك (`unsubscribe`) وروابط حذف الحساب لحماية المستخدم من النقرات الخاطئة.

#### 5. `worker/src/address.ts`
- **الدور:** مولد أسماء البريد الإلكتروني الآمنة والسهلة القراءة (Pronounceable & Secure Random).
- **المعايير:** إنتاج بادئات تجمع بين مقاطع لفظية متوازنة لغوياً وأرقام عشوائية لضمان تفرد العنوان بنسبة 100% ومنع أي تصادم مع عناوين سابقة.

---

### ثانياً: محرك الواجهة الأمامية والأصول المشتركة (Frontend Engine & Shared Assets)

#### 1. `index.html` & `public/index.html`
- **الدور:** الصفحة الرئيسية للمنصة باللغة العربية (الافتراضية).
- **المكونات:**
  - **الترويسة (Header):** هوية العلامة التجارية المتطورة `FreeTemp.email` مع أيقونة البريد المضمنة بتنسيق SVG فائق الدقة، وشارات التوجيه السريع.
  - **منطقة صندوق البريد الحي (Hero & Mailbox Container):**
    - حقل عرض الإيميل المُنشأ فوراً مع زر النسخ بزر واحد.
    - مؤقت عد تنازلي دائري حي يعرض الدقائق والثواني المتبقية.
    - أزرار التحكم السريع: زر التمديد (`+10 دقائق`) وزر الإتلاف الفوري (`إتلاف الصندوق / Burn`).
    - شارة حالة الاتصال الحي (Live WSS Connected / Reconnecting).
  - **صندوق الوارد (Inbox Area):**
    - حالة الانتظار (Empty State Animation) مع دائرة انتظار تفاعلية.
    - قائمة الرسائل الواردة مع تمييز فوري لرمز التحقق (OTP Card) بألوان متباينة وإمكانية نسخه بضغطة واحدة دون الحاجة لقراءة كامل الرسالة.
    - قارئ الرسائل الكامل (Email Viewer Modal / Full-view) مع تقنية التعقيم التام وعزل وسوم الـ HTML غير الآمنة.
  - **كتل المحتوى المعرفي والسيو:**
    - شرح شامل لآلية عمل البريد المؤقت وحالات الاستخدام الواقعية (التسجيل في المواقع، تجنب السبام، حماية الخصوصية).
    - جدول مقارنة تفصيلي بين البريد المؤقت والبريد الدائم.
    - قسم الأسئلة الشائعة (FAQ Accordion) المدعوم ببيانات Schema.org المنظمة.

#### 2. `shared/app.js` & `public/shared/app.js`
- **الدور:** المحرك التشغيلي لكامل الواجهة التفاعلية بدون أي مكتبات ثقيلة (Pure Vanilla JavaScript)، ما يوفر زمن تحميل شبه فوري (Zero Framework Overhead).
- **أبرز المميزات:**
  - **إدارة دورة الحياة (Session & Lifecycle Management):** حفظ العنوان والتوكن ووقت الانتهاء في `sessionStorage` لمنع فقدان الجلسة عند تحديث الصفحة من قبل المستخدم.
  - **نظام الاتصال المزدوج الموثوق (Hybrid WebSocket + Polling Fallback):**
    - الاتصال الأساسي عبر WebSocket الحي مع تبادل إشارات دورية `ping/pong` كل 30 ثانية.
    - في حال انقطاع الشبكة أو حجب بروتوكول WSS من قبل جدار حماية محلي، يقوم المحرك تلقائياً بالتحول إلى نظام الاستطلاع الدوري الذكي (Exponential Backoff Polling) لضمان عدم ضياع أي رسالة.
  - **تنبيهات صوتية واهتزاز تفاعلي (Web Audio API & Vibration API):**
    - توليد نغمة تنبيه نقية فور وصول رسالة جديدة دون الحاجة لتحميل ملفات صوتية خارجية بصيغة MP3، مما يضمن عمل التنبيه الصوتي حتى مع سرعات الإنترنت الضعيفة.
    - تشغيل نمط اهتزاز مخصص `navigator.vibrate([100, 50, 100])` على الهواتف الذكية.
  - **التعقيم وعزل الأمان (DOMPurify Sandboxing):**
    - تنظيف كود الـ HTML للرسالة عبر `DOMPurify.sanitize()` مع منع تشغيل أي سكربتات خبيثة (`<script>`, `<iframe>`, `onload`, `onerror`).
    - استبدال سمات الصور `src` تلقائياً بسمة بديلة `data-blocked-src` لحجب بكسلات التجسس، وتوفير زر آمن يسمح للمستخدم بعرض الصور برغبته.

#### 3. `shared/i18n.js` & `public/shared/i18n.js`
- **الدور:** محرك التدويل وتعدد اللغات.
- **التغطية:** يدعم 22 لغة عالمية دعماً كاملاً شاملاً للترجمة وخصائص الاتجاه (`dir="rtl"` و `dir="ltr"`):
  - العربية (ar)، الإنجليزية (en)، الإسبانية (es)، الفرنسية (fr)، الألمانية (de)، الإيطالية (it)، البرتغالية (pt)، الروسية (ru)، اليابانية (ja)، الصينية المبسطة (zh)، الكورية (ko)، التركية (tr)، الهندية (hi)، الإندونيسية (id)، الهولندية (nl)، البولندية (pl)، السويدية (sv)، الفيتنامية (vi)، التايلاندية (th)، التشيكية (cs)، اليونانية (el)، العبرية (he).
- **الميزات:** كشف تلقائي للغة المتصفح، دعم التبديل الفوري بدون إعادة تحميل الصفحة، وحفظ التفضيل في `localStorage`.

#### 4. `shared/theme.js` & `public/shared/theme.js`
- **الدور:** إدارة المظهر اللوني (Dark / Light Theme).
- **الميزات:**
  - مزامنة فورية مع إعدادات نظام التشغيل للمستخدم (`prefers-color-scheme`).
  - منع وميض الشاشة أثناء التحميل (Flash of unstyled theme prevention).
  - إضافة فئة `dark` على جذر الوثيقة وتحديث أيقونات المظهر في الترويسة بمرونة.

#### 5. `shared/style.css`
- **الدور:** الأنماط التكميلية المخصصة (Tailwind Custom Extensions).
- **المحتوى:** تخصيصات شريط التمرير (Scrollbar styling)، حركات النبض والوميض لشارة الاتصال الحي (Pulse Animations)، ومؤثرات الزجاج الشفاف (Glassmorphism & Backdrop Blur).

---

### ثالثاً: المقالات والأدلة التقنية وصفحات الهبوط (Articles & Static Architecture)

#### 1. `article-template.html` و `article.html`
- **الدور:** نموذج العرض القياسي فائق التحسين للسيو للمقالات التقنية والأدلة المعمارية.
- **الميزات:**
  - هيكل قراءة مريح وسريع، متوافق مع معايير Google Core Web Vitals.
  - قائمة محتويات تفاعلية (Table of Contents).
  - بطاقة مؤلف معتمدة تابعة لفريق هندسة أمان المنصة (`FreeTemp.email Security Engineering Group`).
  - كود بيانات منظمة `TechArticle` يشمل تاريخ النشر، التعديل، والكلمات المفتاحية.

#### 2. مستودع المقالات التقنية (`ar/articles/` و `en/articles/`)
يحتوي المشروع على **92 مقالاً هندسياً معمقاً** (46 مقالاً بالعربية و 46 مقالاً بالإنجليزية) تم بناؤها وتوليدها لتغطي مواضيع حيوية تبحث عنها محركات البحث والمهندسون، ومن أبرزها:
- `universal-verification-coverage.html`: التغطية الشاملة لرسائل التحقق وفك شفرات OTP.
- `combating-marketing-trackers-and-spy-pixels.html`: مكافحة بكسلات التجسس وتتبع البريد.
- `zero-knowledge-inbox-architecture.html`: البنية الصفرية للمعرفة وعزل البيانات في الذاكرة.
- `smtp-handshake-protocol-inbox-architecture.html`: بروتوكول مصافحة SMTP واستقبال الرسائل على الحافة.
- `preventing-credential-stuffing-and-data-breaches.html`: حماية الحسابات من التسريبات وحشو بيانات الاعتماد.
- `developer-guide-headless-testing-api.html`: دليل المطورين للاختبار المؤتمت للبريد المؤقت عبر Cypress و Playwright.

#### 3. الصفحات التوثيقية المساندة (`ar/` و `en/`)
- `guide.html`: الدليل الإرشادي الشامل لاستخدام المنصة خطوة بخطوة للمبتدئين والمحترفين.
- `faq.html`: أكثر من 20 سؤالاً وجواباً مفصلاً تغطي الصلاحية، الأمان، استعادة الحسابات، وقانونية الخدمة.
- `about.html`: نبذة عن المنصة وفلسفتها الهندسية والتزامها بحرية الخصوصية الرقمية.
- `privacy.html`: وثيقة سياسة الخصوصية الصارمة المتوافقة مع تشريعات GDPR و CCPA.
- `blog.html`: فهرس الأرشيف التقني ومكتبة المقالات الكاملة مع تصنيف المقالات حسب الفئات.

---

### رابعاً: سكربتات الأتمتة والبناء (Scripts & Automation Engine)

#### 1. `scripts/build-routers.ts`
- **الدور:** سكربت مؤتمت بلغة TypeScript يقوم بتوليد **29 صفحة توجيه جذرية متعددة اللغات** لخدمة محركات البحث في كافة بلدان العالم. يضمن أن يدخل كل مستخدم أجنبي على صفحة مخصصة بلغته تحتوي على الترويسات الصحيحة وعلامات الكانونيكال الخاصة بها.

#### 2. `scripts/generate-static-articles.cjs`
- **الدور:** المحرك التوليدي للمقالات الثابتة، يقرأ بيانات المقالات ويحقنها داخل قوالب الـ HTML مع حساب زمن القراءة وعلاقات الروابط ذات الصلة وتضمين أكواد Schema.org تلقائياً لكل مقال.

#### 3. `scripts/generate-sitemap.ts`
- **الدور:** مولد خريطة الموقع العملاقة `sitemap.xml`.
- **الميزات:** يضم كافة الروابط للموقع (الصفحات الرئيسية، الأدلة، الأسئلة الشائعة، وكافة المقالات الـ 92)، مع مصفوفة الروابط التبادلية `xhtml:link rel="alternate" hreflang="..."` لكل لغة، وتحديد أولويات الزحف (`priority`) وتكرار التحديث (`changefreq`).

#### 4. `scripts/verify-faq-seo.ts`
- **الدور:** أداة فحص واختبار جودة السيو لصفحات الأسئلة الشائعة والتحقق من صحة كود الـ JSON-LD والوسوم التوضيحية قبل النشر.

---

### خامساً: ملفات الإعدادات والبيئة (Configuration & Tooling)

#### 1. `package.json`
- **الدور:** إدارة التبعيات والأوامر التشغيلية للمشروع.
- **الحزم الرئيسية:**
  - `vite`: بيئة التطوير فائقة السرعة للبناء وتجميع الأصول.
  - `tailwindcss` و `@tailwindcss/vite`: إطار عمل التنسيقات الحديث.
  - `postal-mime`: حزمة تحليل رسائل البريد الخام منخفضة استهلاك الذاكرة.
  - `dompurify` و `@types/dompurify`: مكتبة تعقيم وتنظيف كود HTML.
  - `tsx` و `typescript`: لتشغيل وفحص سكربتات TypeScript مباشرة.

#### 2. `vite.config.ts`
- **الدور:** إعداد بيئة بناء Vite، وضبط مسارات الإخراج وتجميع الأصول داخل مجلد `dist/`.

#### 3. `robots.txt`
- **الدور:** توجيه محركات البحث، السماح بالزحف لكافة الصفحات العامة المفهرسة، وتوجيه العناكب مباشرة إلى رابط خريطة الموقع `https://freetemp.email/sitemap.xml`.

#### 4. `sitemap.xml`
- **الدور:** خريطة الموقع الكاملة المعتمدة لدى Google Search Console و Bing Webmaster Tools.

---

## 4. المراجعة الأمنية والامتثال (Security & Privacy Audit)

| المعيار الأمني | التطبيق في FreeTemp.email | درجة التقييم |
| :--- | :--- | :---: |
| **منع التجسس على الصناديق (Anti-Eavesdropping)** | توليد رمز سري مشفر لكل صندوق لا يمكن استعراض الرسائل أو فتح WSS بدونه. | **ممتاز (A+)** |
| **الحماية من هجمات XSS** | فحص وتعقيم كافة الرسائل باستخدام `DOMPurify` داخل الـ Frontend، وتجريد السكربتات المضمنة. | **ممتاز (A+)** |
| **حظر بكسلات التعقب (Anti-Spy Pixels)** | استبدال وسوم الصور تلقائياً وحجب التحميل الخارجي الافتراضي لحماية عنوان IP للمستخدم. | **ممتاز (A+)** |
| **حماية الذاكرة والفيض (OOM / DoS Guard)** | تحديد سقف أقصى لحجم الرسالة المقبولة وتحديد حد أقصى للرسائل (50 رسالة لكل صندوق). | **ممتاز (A+)** |
| **تحديد معدل الطلبات (Rate Limiting)** | حماية مسار إنشاء الصناديق عبر عداد الذاكرة في Worker والجاهزية للربط مع Cloudflare Turnstile. | **ممتاز (A+)** |
| **التخزين المؤقت وحتمية الإتلاف (Ephemeral Destruction)** | إتلاف فوري عبر جدولة الألارم (Alarms API) وزر الإتلاف الفوري (Burn Mailbox). | **ممتاز (A+)** |

---

## 5. مصفوفة السيو والبيانات المنظمة (SEO & Schema Specifications)

تتضمن جميع صفحات الموقع حزمة بيانات وصفية معتمدة وفق أرقى معايير البحث العالمية:
1. **`WebSite Schema`:** تعريف المنصة، النطاق `freetemp.email`، والأسماء البديلة (`FreeTemp`, `بريد مؤقت`, `Temp Mail`).
2. **`SoftwareApplication Schema`:** تعريف التطبيق كأداة ويب خدمية ومجانية بالكامل (`price: 0`, `ApplicationCategory: UtilitiesApplication`).
3. **`Organization Schema`:** تعريف الهوية التحريرية وفريق الأمان واللوجو الرسمي.
4. **`FAQPage Schema`:** تحويل الأسئلة والأجوبة في صفحة الـ FAQ إلى نتائج بحث غنية (Rich Snippets) تظهر مباشرة في نتائج محرك بحث Google.
5. **`TechArticle Schema`:** إدراج كل مقال تقني مع الوسوم الدلالية المحددة لزيادة معدل النقر للظهور (CTR).

---

## 6. دليل التشغيل والربط الميداني (Operations & Domain Deployment)

### الخطوة الأولى: ربط نطاق الموقع في Cloudflare
1. توجيه الـ DNS لنطاق `freetemp.email` إلى خوادم Cloudflare.
2. تفعيل خيار **Cloudflare Email Routing** للنطاق.
3. إضافة قاعدة التقاط شاملة (Catch-All Rule) وتوجيهها لتشغيل Worker الخاص بالمشروع (`temp-mail`).

### الخطوة الثانية: نشر الـ Worker والـ Durable Objects
من داخل مجلد `worker/` قم بتنفيذ:
```bash
cd worker
npx wrangler deploy
```
سيقوم Wrangler بحجز كلاس الـ Durable Objects وتفعيل محرك معالجة الرسائل وتعيين نطاقات CORS.

### الخطوة الثالثة: إضافة نطاقات بريد إضافية مستقبلاً (Multi-domain Expansion)
عند رغبتك في تزويد المنصة بنطاقات جديدة للبريد الوارد (مثل `inbox.example.com` أو `mailtemp.xyz`):
1. أضف النطاق الجديد في حساب Cloudflare وفعل عليه Email Routing.
2. في ملف `worker/src/address.ts` قم بتوسيع مصفوفة النطاقات المتاحة:
   ```typescript
   export const AVAILABLE_DOMAINS = [
     'freetemp.email',
     'your-new-domain-1.com',
     'your-new-domain-2.com'
   ];
   ```
3. أعد نشر الـ Worker عبر `npx wrangler deploy`، وسيقوم النظام بتوزيع العناوين عشوائياً أو إتاحة خيار للمستخدم لاختيار النطاق المفضل من القائمة المنسدلة.

---

## 7. الخاتمة والتقييم النهائي

يمثل مشروع **FreeTemp.email** حالة نموذجية لتطبيقات الويب الحديثة فائقة السرعة والأمان؛ حيث يجمع بين صغر الحجم، انعدام التكاليف التشغيلية الثابتة (Serverless Pay-per-use / Free Tier Compatible)، والحصانة الأمنية الفائقة، مع أداء استثنائي لمحركات البحث وترتيب الكلمات المفتاحية العالمية.

> تم إعداد وتوثيق هذه المراجعة الفنية كمرجع شامل لكل مهندس أو مطور يدير أو يطور هذا المشروع.
